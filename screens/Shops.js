import React from 'react'
import { View, Text } from 'react-native'
import colors from '../assets/colors/colors'

const Shops = () => {
  return (
    <View style={{ backgroundColor: colors.bg_2, alignItems: 'center', justifyContent: 'center', flex: 1 }}>
      <Text style={{ color: colors.btn, fontSize: 18, width: '100%', textAlign: 'center' }}>This screen is not accesible right now...</Text>
    </View>
  )
}

export default Shops
